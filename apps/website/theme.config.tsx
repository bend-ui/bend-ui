import { Colors } from './src/components/token-docs/colors';
import type { DocsThemeConfig } from './src/nextra';

const config: DocsThemeConfig = {
  components: {
    Colors,
  },
};

export default config;
