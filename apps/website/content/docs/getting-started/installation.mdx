---
title: Installation
description: Learn how to install and set up Bend UI Design System in your project
---

# Installation

Get started with Bend UI by installing the necessary packages in your project.

## Prerequisites

Before installing Bend UI, ensure you have:

- **Node.js**: Version 18 or higher
- **Package Manager**: npm, yarn, or pnpm
- **React**: Version 18 or higher

## Installation Steps

### 1. Install Packages

Install the core Bend UI packages:

```bash
npm install @bend-ui/react @bend-ui/preset
```

Or using yarn:

```bash
yarn add @bend-ui/react @bend-ui/preset
```

Or using pnpm:

```bash
pnpm add @bend-ui/react @bend-ui/preset
```

### 2. Configure Panda CSS

Bend UI uses Panda CSS for styling. Create or update your `panda.config.ts`:

```typescript
import { defineConfig } from '@pandacss/dev';
import { createBendPreset } from '@bend-ui/preset';

export default defineConfig({
  preflight: true,
  presets: [createBendPreset()],
  include: [
    './src/**/*.{js,jsx,ts,tsx}',
    './app/**/*.{js,jsx,ts,tsx}',
    './node_modules/@bend-ui/react/**/*.{js,jsx,ts,tsx}',
  ],
  exclude: [],
  outdir: 'styled-system',
});
```

### 3. Add Panda CSS Scripts

Add the following scripts to your `package.json`:

```json
{
  "scripts": {
    "prepare": "panda codegen",
    "dev": "panda --watch & next dev"
  }
}
```

### 4. Generate Styles

Run the Panda CSS codegen:

```bash
npm run prepare
```

### 5. Import Styles

Import the generated Panda CSS styles in your app:

```typescript
import 'styled-system/styles.css';
```

## Verify Installation

Create a test component to verify everything is working:

```tsx
import { Button } from '@bend-ui/react';

export default function App() {
  return <Button>Hello Bend UI!</Button>;
}
```

## Next Steps

- [Quick Start](/docs/getting-started/quick-start) - Build your first page with Bend UI
- [Configuration](/docs/getting-started/configuration) - Customize your setup
- [Theming](/docs/foundation/theming) - Learn about customizing the design system
