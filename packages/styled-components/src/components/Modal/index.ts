import { Body } from './Body';
import { CloseButton } from './CloseButton';
import { Footer } from './Footer';
import { Root } from './Modal';
import { Header } from './ModalHeader/Header';

export const Modal = Object.assign(Root, { Header, Body, Footer, CloseButton });
