import { createStyles } from '../../styles';

export default createStyles({
  root: {
    display: 'inline-flex',
    backgroundColor: '$emerald500',
    padding: '4px 8px',
    borderRadius: '100px',
    color: 'white',
  },
});
