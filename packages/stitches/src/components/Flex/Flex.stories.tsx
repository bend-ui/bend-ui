import { Flex } from './';
import type { StoryObj, Meta, StoryFn } from '@storybook/react';

export default {
  title: 'Layout/Flex',
  component: Flex,
} as Meta<typeof Flex>;

export const Default: StoryObj<typeof Flex> = {};
