import { createStyles } from '../../styles';

export default createStyles({
  root: {
    display: 'flex',
    border: '1px solid $border-default',
    borderRadius: '$md',
    maxInlineSize: 'min(45ch, 90vw)',
  },
});
