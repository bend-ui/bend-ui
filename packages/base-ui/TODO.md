# Base UI Component TODOs

Small component improvements noticed while wiring the dental-management and
ski-resort demos.

## Completed

- [x] Align `Dialog.Trigger` with `Button`.
- [x] Improve `Avatar` ergonomics.
- [x] Add a recipe-backed `Badge`.
- [x] Add a styled `Table` wrapper.
- [x] Tighten `ScrollArea` defaults.
- [x] Document layout patterns for `Input`.
- [x] Add a shared `Surface` pattern.
- [x] Add segmented filter controls.
- [x] Add a metric/KPI card pattern.
- [x] Add timeline/list item patterns.
- [x] Add action row / button list examples.
- [x] Document responsive Panda prop limits.
- [x] Add page header / dashboard shell patterns.

The concrete component APIs live under `src/components`, with the operational
layout patterns grouped in `src/components/Patterns`.
