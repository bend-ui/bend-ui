{
  "name": "ariakit",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/ariakit/src",
  "projectType": "library",
  "tags": ["ui"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "tsup",
        "cwd": "packages/ariakit"
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "{projectRoot}/../../coverage/packages/ariakit"
      }
    },
    "storybook": {
      "options": {
        "port": 4407,
        "config-dir": ".storybook"
      },
      "configurations": {
        "ci": {
          "args": ["--quiet"]
        }
      }
    },
    "build-storybook": {
      "outputs": ["{workspaceRoot}/dist/storybook/ariakit"],
      "options": {
        "config-dir": ".storybook",
        "output-dir": "../../dist/storybook/ariakit"
      },
      "configurations": {
        "ci": {
          "quiet": true
        }
      }
    },
    "test-storybook": {
      "executor": "nx:run-commands",
      "options": {
        "command": "test-storybook -c packages/ariakit/.storybook --url=http://localhost:4407"
      }
    },
    "static-storybook": {
      "executor": "@nx/web:file-server",
      "dependsOn": ["build-storybook"],
      "options": {
        "buildTarget": "ariakit:build-storybook",
        "staticFilePath": "dist/storybook/ariakit",
        "spa": true
      },
      "configurations": {
        "ci": {
          "buildTarget": "ariakit:build-storybook:ci"
        }
      }
    }
  }
}
