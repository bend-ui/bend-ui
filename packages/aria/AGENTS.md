# `@bend-ui/aria`

Bend UI package adapter over **React Aria Components** (`react-aria-components`).

> **Pending rename (issue #110):** this package is scheduled to become
> `@bend-ui/react-aria` (`packages/react-aria`). Update the name references here once the
> rename lands.

- **Upstream library**: React Aria Components — https://react-spectrum.adobe.com/react-aria/components.html
- **Pinned version**: see `react-aria-components` in `package.json` (source of truth).

## Where to look

- **How to build components here** (structure, styled `cx` + recipe pattern, plain
  re-exports, adding a component, missing-component checks): the `component-libraries` skill.
- **What's implemented and how complete it is** (product intent, progress, quality
  coverage, verified-against-upstream marker): `docs/component-catalog.md`.

This adapter is sequenced after the Base UI v0 slice. This file is intentionally thin.
