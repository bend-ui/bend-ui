import { Meta } from '@storybook/react';

export default {
  title: 'Patterns/Marketing/Hero',
  parameters: {
    layout: 'fullscreen',
  },
} as Meta;

export const SplitImage = () => {
  return <div>Hero</div>;
};
