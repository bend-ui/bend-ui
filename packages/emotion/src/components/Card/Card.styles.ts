import { createStyles } from '../../styles';

export default createStyles(() => ({
  root: {
    p: '$lg',
    bg: '$surface',
    borderWidth: '1px',
    borderColor: '$gray700',
    color: '$on-surface',
    borderRadius: '$lg',
    boxShadow: '$DEFAULT',
  },
}));
