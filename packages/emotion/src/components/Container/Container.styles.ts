import { createStyles } from '../../styles';

export default createStyles({
  root: {
    marginInline: 'auto',
    maxWidth: '1280px',
    $bp1: {
      maxWidth: '1068px',
    },
    $bp2: {
      maxWidth: '1280px',
    },
  },
});
