import { createStyles } from '../../styles';

export default createStyles({
  root: {
    backgroundColor: '$surface',
    borderWidth: '1px',
    borderStyle: 'solid',
    borderColor: '$outline',
    borderRadius: '$md',
    boxShadow: '$sm',
    p: '$xs',
    zIndex: 1,
  },
});
