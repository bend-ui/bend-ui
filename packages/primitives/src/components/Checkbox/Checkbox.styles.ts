import type { CSSProperties } from 'react';

export const checkboxStyles: Record<string, CSSProperties> = {
  root: {
    display: 'inline-flex',
    flexDirection: 'row',
    alignItems: 'center',
  },
};
