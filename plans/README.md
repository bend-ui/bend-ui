# Implementation Plans

Generated by the improve skill on 2026-06-14. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Restore the published package build baseline | P1 | S-M | - | TODO |
| 002 | Upgrade vulnerable production dependencies | P1 | M | 001 | TODO |
| 003 | Normalize public package export metadata | P1 | M | 001 | TODO |
| 004 | Add CI export validation for public packages | P1 | S | 001, 003 | TODO |
| 005 | Add wrapper characterization tests for active component packages | P2 | M-L | 001 | TODO |
| 006 | Remove React foundation through Base UI low-level migration | P1 | L | 001, 005 | TODO |
| 007 | Plan upstream adapter component catalogs | P1 | M | 006 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 should land first because 002, 003, 004, and 005 rely on trustworthy package builds.
- 003 should land before 004 because CI export validation needs package metadata and `check-exports` targets to be normalized first.
- 002 can run after 001 and in parallel with 003 if the executor coordinates lockfile/package manifest changes carefully.
- 005 can run after 001; it is intentionally independent of the packaging plans.
- 006 should run after 005 so the public Base UI component behavior has characterization coverage before the low-level component migration.
- 007 should run after 006 so the upstream-first catalog plan assumes the React foundation removal and Base UI-local component direction.

## Findings considered and rejected

- Committed `dist` artifacts: rejected. `dist` directories exist locally but `git ls-files 'packages/*/dist/*'` returned no tracked files.
- Full CLI command-flow redesign: deferred. It is a real direction/correctness finding, but lower leverage than restoring builds, dependency security, packaging checks, and wrapper coverage.
- Website docs cleanup: deferred. It is valuable, but should follow export/API normalization so docs point at the settled public surface.
- Nx Cloud token handling: deferred as a short security cleanup. If the token has write scope, rotate it and move it out of source; do not copy the token value into any plan, issue, or PR.
